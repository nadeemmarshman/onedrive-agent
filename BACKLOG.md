# OneDrive Agent — Product Backlog & Daily Scrum Log

**Purpose:** Agile/Scrum-style tracking artifact for this project, forming
part of the portfolio of evidence. Demonstrates lightweight application of
Scrum practices to a solo technical build — adapted deliberately, with the
adaptation made explicit (see footnotes) rather than presented as textbook
Scrum, since this project does not run fixed time-boxed Sprints.

## Update log (this file)

| Date | Change |
|---|---|
| 2026-06-28 | File created as "Backlog & Daily Standup Log"; Phase 3 closeout checklist item and test-pack backlog item #1 logged. |
| 2026-06-28 | Retitled to correct Scrum terminology ("Daily Scrum" rather than the colloquial "standup"); retrospective-style questions folded into each Daily Scrum entry rather than run as a separate ceremony, since this project has no fixed Sprint boundary for a true Sprint Retrospective; methodology footnotes added throughout; applied retroactively to the existing entry below without altering its original date or content, only its structure/labeling. |
| 2026-06-28 | Corrected the Daily Scrum entry to explicitly note that this file's own creation, and the fact it was not yet committed/pushed to GitHub, had been stated in chat but omitted from the artifact itself — added as a blocker/status note, since the Daily Scrum log should reflect everything discussed, not just a subset. |

---

## Product Backlog¹

Status values: **Open** (not yet discussed) | **In Discussion** | **Actioned**
(decision made, reflected in handoff doc/README — the Product Backlog item
is then analogous to a closed/delivered Backlog Item²) | **Ratified — Planned
(post-pilot)** (decision made and locked in, including exact wording/spec
where applicable, but implementation deliberately deferred until after
pilot sign-off — distinct from Actioned, where the change is already
reflected in the live handoff doc/README) | **Deferred** (explicitly out
of scope for now, with reason) | **Rejected** (considered, not adopted,
with reason)

| # | Raised | Item | Status | Notes |
|---|---|---|---|---|
| 1 | 2026-06-28 | Testing strategy and test case design as a separate deliverable: develop test packs, segregated "on paper" by functionality (a design artifact distinct from the actual test code). Add a dedicated test pack for state-transition test cases (tied to the Phase 4.5 state-machine design). | Actioned | Completed 2026-07-05. Delivered as `TEST_STRATEGY.md` (on-paper coverage map of all 67 tests by functionality, the state-transition test pack, and gaps G1–G3) and `TEST_BED_AND_CASES.md` (handover-grade test-bed construction guide with green/red-line cases + answer key). Surfaced gap G1 (loop termination untested) — closed via new `test_phase8_pre_pilot.py` (3 unit + 1 integration, verified green) — plus a fixture-integrity guard for the recurring I5 class. Done as part of the Phase 8 DoR gate. |
| 2 | 2026-06-28 | Frame Phase 4.5's restart-and-resume mechanism explicitly as a state machine (named states + transitions + persisted current-state), rather than ad hoc retry/variable-saving logic — including the safety property that `AWAITING_HUMAN_APPROVAL` must never be silently skipped past on resume. | Actioned | Added to handoff doc v4.0, "Note on Phase 4.5" section, with a proposed state diagram. Implementation deferred to when Phase 4.5 is actually built — this entry records the design decision, not the code. |
| 3 | 2026-07-01 | Build a formal, committed test suite (`test_phase3_4_45.py`) covering Phases 3, 4, and 4.5 — with explicit negative/red-line test cases for each failure boundary. Must be completed **before Phase 5**, since Phase 5 introduces destructive-action execution and solid negative test coverage must exist before that gate is built. Negative test cases to cover at minimum: corrupted/invalid `agent_state.json`, unrecognised state name on resume, `SENDGRID_API_KEY` set but invalid (graceful failure, not crash), disk write failure on state persistence, Claude returning `None` as tool argument value, `AWAITING_HUMAN_APPROVAL` safety guarantee under resume, and all API error branches (auth, rate limit, connection, status). Unit tests per function first, then integration tests for the chain, per the standing two-layer testing rule. | Actioned | Completed 2026-07-01 22:47 (34 tests). Extended 2026-07-02 with Phase 5 approval gate tests: now 40 unit + 5 integration = 45 total, all passing. |
| 4 | 2026-07-01 | Enrich `alert_on_error()` in `resilience.py` with per-error-type troubleshooting instructions — not just "something broke, check the log" but "here is the most likely cause and the first place to look." Each alert category (auth failure, rate limit, connection failure, insufficient credit, resume into `AWAITING_HUMAN_APPROVAL`, corrupted state file) should include a specific, actionable resolution step in the email body. Particularly important for load-shedding / middle-of-the-night resume scenarios where quick diagnosis matters. Each troubleshooting path should have a corresponding negative test case in item #3's test suite. | Actioned | Completed 2026-07-01 22:47. 7 named alert functions built, each structured around the 5W incident-management framework (WHAT/WHEN/WHERE/TRIGGERED BY/WHY/STEPS) with graduated urgency, exact commands to run, README-reference pattern, and masked user ID (`_masked_user_id()`) in every TRIGGERED BY field. RAID R5 added for residual credential exposure risk. |
| 5 | 2026-07-02 | User roles and assignment — define who can trigger the agent, who receives alerts, who has approval authority for destructive actions, and whether these should be the same person or separable (e.g. an operator runs the agent, a senior approver authorises deletions). Consider whether the approval gate should support named approvers, an approval audit trail with usernames, or role-based routing of alerts to different distribution lists. | Actioned | Decision made 2026-07-02: single-user model adopted — the person running the agent is also the approver. Proportionate for a single-user portfolio project; multi-role model (operator vs. approver separation, role-based alert routing, named approver audit trail) logged as a deliberate future consideration, not an unconsidered gap. Recorded in handoff doc Decision log (item 6) and to be noted in README governance section. |
| 6 | 2026-07-02 | Phase 8 — UAT/Pilot stage. Validate the agent against a real, limited subset of live OneDrive data (not the full folder) immediately before full production rollout. Need to: select a specific, contained OneDrive subfolder; update `starting_folder` in `agent_loop.py` and `test_phase5_live.py`; run the agent end-to-end with the approval gate; review the pre-run snapshot and results; sign off before expanding to the full OneDrive folder. | Actioned | **Signed off by {{owner}}, 2026-07-20 05:46** — the final step this item names. Full trail: DoR gate (2026-07-05) → audit-control regime (2026-07-08/09) → pilot Sessions 1/2/2b (2026-07-15–19, all six test cases closed) → `PILOT_SIGNOFF_SUMMARY.md` (2026-07-19) → formal sign-off (2026-07-20). Terminology deliberately UAT/Pilot, not POC — the POC stage was effectively already covered by Phases 1–4 against synthetic `sample_data/`. Full OneDrive rollout is now unblocked in principle; not itself scheduled as part of this backlog. |
| 7 | 2026-07-15 | Add a command-line folder argument to `agent_loop.py` (and align `test_phase5_live.py`), replacing the hardcoded `sample_data` default — surfaced during the pilot dry-run, worked around via a parameter one-liner (see handoff Decision log, 2026-07-15). | Open | Deliberately a post-pilot change — no code modifications mid-pilot. |
| 8 | 2026-07-15 | Produce a one-page pilot sign-off summary at the end of Phase 8: checkpoints and results, defects found/fixed (I9), coverage notes including the RB-06/RB-09 separate targeted runs and the A10 image-coverage caveat in the sign-off wording. | Actioned | Delivered 2026-07-19 as `PILOT_SIGNOFF_SUMMARY.md` (commit `f75b241`, live-verified). CP1/CP2/CP3 results, all 19 GB/RB case outcomes, defects I9/I10/I11, RB-13's honest disclosure (behavioral property verified, literal resume mechanism unproven — R1 Monitoring), and known limitations (A10, Backlog #9/10/11) all covered per spec — coverage notes also extended beyond RB-06/RB-09 to include RB-11 (also a standalone targeted fallback run). Recommended sign-off — **which {{owner}} formally gave 2026-07-20 05:46** (see Backlog #6). |
| 9 | 2026-07-19 | **RB-10 handle-vs-accept decision (gap G3, RAID I15):** the agent's content-hash read transparently hydrates OneDrive online-only placeholders (correct dedup results, but silently downloads every placeholder scanned — bandwidth/disk cost, and undoes the user's "free up space" choice; material at Documents-rollout scale). Options considered: (a) **accept** as a documented known limitation; (b) **handle** — always skip/flag placeholders; **(c) opt-in, skip-by-default** — skip placeholders from duplicate comparison by default (not hashed, not hydrated, user told how many were skipped and why), with a new `--include-online-only` command-line flag for explicit full coverage, gated behind a clear data-cost warning and y/n confirmation before any hydration. | Ratified — Planned (post-pilot) | **Ratified by {{owner}} 2026-07-19: Option C** (opt-in flag, skip-by-default) — matches the project's existing safety pattern (nothing happens without explicit user intent; same spirit as the approval gate and snapshot precondition) while leaving full coverage available. **Does not block pilot sign-off** — RB-10/I15 proved correctness is unaffected by current hydration behaviour; this is a planned post-pilot enhancement, not a defect fix. **Technical insertion point (preserve for the implementer):** `scan_folder()` reads metadata only and does not hydrate — hydration happens at the hash step inside `find_duplicates()`; the fix sits between the scan and the hash call, not inside the scan. **Ratified message wording (verbatim; line breaks adapted to this table's cell format; replace `12` with the live count at runtime):** *Default mode, when online-only files are found and skipped:* "⚠️ 12 file(s) are online-only (stored in the cloud, not on this device) and were not scanned for duplicates, to avoid unexpected downloads.<br>To include them in the comparison, re-run with --include-online-only." *Opt-in mode (`--include-online-only`), shown before hydration, requires y/n confirmation:* "⚠️ Full duplicate check requested. This will download the content of 12 online-only file(s) from OneDrive so they can be compared — this counts against your data usage and may take a while depending on file sizes and your connection.<br>Recommended: run this on Wi-Fi rather than mobile data or a capped connection, and expect it to take longer than a normal scan — larger files or many placeholders can add real time, not just data.<br>Continue and download these files now? (y/n)" |
| 10 | 2026-07-19 | **Wire real state-machine persistence into the live path (RAID I16, R1 reopen).** `resilience.py`'s `save_state()`/`check_for_resume()` exist and are correctly unit-tested in isolation, but no live-runnable script (`agent_loop.py`, `approval_gate.py`, `test_phase5_live.py`) actually calls them — so `AWAITING_HUMAN_APPROVAL` is never persisted to `agent_state.json` during a real run. RB-13's redefined behavioural test (I16) proved the practical safety outcome holds anyway (an unplanned crash never results in unapproved execution, via "always restart clean"), but the originally-designed resume-from-saved-state mechanism remains unproven and not live. Post-pilot task: wire `save_state()` calls at each transition in `agent_loop.py`/`approval_gate.py`'s live path, and `check_for_resume()` on startup, so a restart can land back in `AWAITING_HUMAN_APPROVAL` for real rather than always re-scanning from scratch. | Open | Deliberately post-pilot — same reasoning as Backlog #9: no code changes mid-pilot. Does not block sign-off (I16 already proves the practical safety property); this closes the gap between "proven in a unit test" and "proven live." |
| 11 | 2026-07-19 | **Diagnostic logging on hash-read failures (RAID I17).** `find_duplicates()` in `tools.py` correctly and gracefully skips a file it can't hash (locked, permission-denied, or an unhydrated OneDrive placeholder) via a bare `except (OSError, PermissionError): continue` — but silently, with no log line or message stating which file was skipped or why. RB-11 (I17) proved the skip itself is safe (no crash, unaffected files still processed correctly), but a real user watching a run would have no way to know a visible file was quietly excluded from comparison. Post-pilot task: add a log/print line at the skip point (e.g. `logger.warning(f"Skipped {path}: could not read ({e})")`) naming the file and the reason. | Open | **Low priority, UX polish.** Deliberately post-pilot — same reasoning as Backlog #9/#10: no code changes mid-pilot. Does not block sign-off (I17 already proves the underlying safety property); this is a UX/observability improvement, not a defect fix. |
| 12 | 2026-07-19 | **Executive project summary** — a recruiter/hiring-manager-facing executive summary of the whole project (not just Phase 8), committed to the repo as `ONEDRIVE_AGENT_EXEC_PROJECT_SUMMARY.docx`, following the same repo-artifact convention as `TEST_USE-CASES_PLAIN_ENGLISH.md`. Process: draft as `.md` first for review, then convert to `.docx` once approved — same two-step pattern already used for `PILOT_SIGNOFF_SUMMARY.md`. | Actioned | Delivered as two commits: `ONEDRIVE_AGENT_EXEC_PROJECT_SUMMARY.md` (`32638eb`, reviewed and approved) and `ONEDRIVE_AGENT_EXEC_PROJECT_SUMMARY.docx` (`6b81e4b`). Reviewing the markdown draft surfaced two real fixes folded in along the way: README.md corrected for a state-machine overclaim (RAID I16) that contradicted its own Phase 8 disclosure, and "Phase 8 complete" tightened to "test execution complete — sign-off pending" across README, this document, and the exec summary, since sign-off itself had not actually been given. Docx generated via `python-docx` (Node/pandoc/LibreOffice unavailable in this environment); verified by Word-COM PDF export and visual page-by-page inspection, catching and fixing an awkward title wrap and a near-empty trailing page before commit. |
| 13 | 2026-07-20 | **Migrate `AI-Agent-Build-Handoff_*.md` into the git repo**, so it gets the same deterministic, diffable version history as every other project document, rather than relying on OneDrive's version history. | Actioned | Reconsidered and actioned 2026-07-24, prompted by a related incident: a Job Search project session flagged suspected corruption in the handoff document's version-history table that turned out to be a stale mounted copy — the exact "which copy is real" ambiguity this migration removes. Delivered as a deliberately partial migration, not the full 33-version archive: the 11 versions this document's own table already labels MAJOR (v2.0, v3.0, v3.1, v4.0, v4.1, v5.0, v6.0, v7.0, v8.0, v9.0), plus v1.0 (founding baseline) and current v9.2 — 12 files, each verified byte-for-byte (md5sum) against its OneDrive source before commit. Now at `Handoff-Briefs/` in the repo (commit `1321513`), live-verified. The 21 MINOR-labeled versions were retired, not preserved — git holds milestones going forward, not full incremental history. OneDrive originals to be replaced with a redirect stub once confirmed. |
| 14 | 2026-07-20 | **Habitual check before saving any version-history table edit: confirm the new/edited row's position against the table's existing chronological order before saving**, not just its content. | Open | Raised directly by RAID I19 — a version-history ordering defect (v9.2 row inserted before v9.1) that is a *repeat* of an already-fixed instance of the identical defect (the v7.0 row documents correcting v5.1 being listed after v6.0). Two occurrences of the same error class means the first fix addressed the symptom that one time, not the underlying habit. This is a process/discipline item, not a code or document change — captured here so it isn't lost, and so a third occurrence would be recognizable as "still hasn't been fixed" rather than "a new, unrelated slip." |
| 15 | 2026-07-30 | **Live-run phase.** Fix `propose_action()`'s keeper-selection logic properly in `tools.py` (Phase 1) — it currently keeps `group[0]` (first file by scan order) as the "original" unconditionally. The live run against the real `OneDrive\Pictures\Camera Roll` folder found this picks the wrong file whenever OneDrive's own sync-conflict naming convention (a `" 1"` suffix) is present, since the suffixed file is the true duplicate, not necessarily the one scanned first. Worked around at the call site for this live-run phase (RAID I21, `propose_camera_roll_dedup.py`); this item is the real Phase-1 code fix. | Open | Deliberately not fixed mid-phase — same reasoning as Backlog #7/#9/#10/#11: no code changes to closed/in-flight phases without a deliberate, separately-scoped change. |
| 16 | 2026-07-30 | **Live-run phase.** Draft the two GitHub Releases pages from the existing tags (`v1.0-foundational-build`, `v2.0-live-run-camera-roll`) — both tags exist locally and are pushed to `origin`; the Releases UI step itself (Repo → Releases → Draft a new release → pick existing tag → publish) is manual and still outstanding. These two Releases URLs are the resume/portfolio deliverable this live-run phase's tagging scheme was designed to produce. | Open | Blocks nothing else; purely a manual GitHub UI step once picked up. |

---

## Daily Scrum Log³

Held at the start of each working session. Three core questions (per the
Scrum Guide's Daily Scrum⁴), with a fourth, retrospective-style question
added to capture continuous-improvement thinking without requiring a
separate, Sprint-boundary-dependent ceremony⁵:

1. **What did we complete or solve since the last session?**
2. **What's planned / where are we headed next?**
3. **Any blockers?**
4. **Retrospective note⁶ — what did we miss, learn, or newly identify** (process or scope) that wasn't yet captured in the project artifacts, and has it been added to the Product Backlog?

| Date | Completed/solved since last session | Planned next | Blockers | Retrospective note |
|---|---|---|---|---|
| 2026-06-28 | Phase 3 confirmed complete in chat (live API call succeeded; error handling added). Phase 4.5 scoped and inserted into the plan (handoff doc v4.0 drafted). This `BACKLOG.md` file itself created and restructured to correct Scrum terminology. | Phase 4 build (core decision-execution loop), gated on Phase 3 closeout being confirmed done by {{owner}}. | Phase 3 closeout checklist outstanding (6 items — doc swap, local file saves, git commit/push). SendGrid account setup outstanding, needed before Phase 4.5. **`BACKLOG.md` itself is not yet committed/pushed to GitHub** — not part of the Phase 3 closeout checklist (didn't exist when that checklist was set), so it doesn't block Phase 4, but should be folded into the next git commit. | Identified that "restart-and-resume" is genuinely state-machine logic, not ad hoc retry logic — added to Product Backlog as a design approach for Phase 4.5, not yet formally scoped into the handoff doc. Also identified that test-case design itself deserves to be a separate, "on paper" deliverable (test packs) rather than only living as test code — added as Backlog item #1. |
| 2026-06-28 | State-machine framing for Phase 4.5 actioned: added to handoff doc v4.0 (still open, not yet version-bumped, since Phase 3's closeout checklist — not this content — is what gates progress). Backlog item #2 marked Actioned. | Still gated on Phase 3 closeout checklist confirmation by {{owner}} before Phase 4 build begins. | Same as above — Phase 3 closeout checklist and SendGrid setup both still outstanding. `BACKLOG.md` still not yet committed to GitHub. | Confirmed the distinction between "raising/discussing an idea" (Backlog) and "deciding and documenting it" (handoff doc) is working as intended — the state-machine idea moved cleanly from Backlog item #2 into the handoff doc's Phase 4.5 section without needing a version bump, since v4.0 was still open. |
| 2026-06-29/30 | Phase 3 closeout checklist worked through with {{owner}}, verified via screenshots at each step (handoff doc v4.0 swap confirmed in Project knowledge + local archive; `decision_loop.py` committed/pushed). New standing rule adopted mid-checklist: verbal "done" confirmations alone are no longer sufficient — screenshot or equivalent visual evidence now required before marking a checklist item confirmed. | Phase 4 build (core decision-execution loop) — checklist now fully closed, no longer gated. | None remaining for Phase 4. SendGrid setup + `SENDGRID_API_KEY` (items 8–9) still outstanding, but these gate Phase 4.5, not Phase 4. | **Caught a real error via the new screenshot-verification rule**: an initial attempt to commit `README.md` and `BACKLOG.md` actually used `echo "..." > file` commands, which overwrote both files with one-line placeholder text instead of adding the real, previously-generated content — confirmed by the commit's `84 deletions(-)` stat and matching file-modified timestamps. Caught before being accepted as "done," corrected by re-downloading and re-saving the real files, then re-committing — verified this time via `git show HEAD:README.md` showing the actual file content (not just commit stats) live on GitHub. Direct, concrete evidence that verbal/stat-only confirmation is insufficient and the screenshot+content-verification rule is justified, not just process overhead. |
| 2026-06-30 | Built `RAID_LOG.md` and `RAID_Log.xlsx` (via `build_raid_log.py`) at {{owner}}'s request — a standard PM control artifact (Risks, Assumptions, Issues, Dependencies) separate from this Scrum tracking file. Retroactively decomposed the placeholder-overwrite incident into proper RAID components (R2 risk, A1 assumption, I1 issue), demonstrating the framework's analytical use rather than just adopting its name. | Phase 4 build, once a short "Phase 3.5" documentation closeout (RAID log files saved/committed, README repo-contents list updated) is confirmed by {{owner}}. | RAID log files not yet saved locally or committed to GitHub. | Caught that the README's "Repo contents" list had silently gone stale — `decision_loop.py` and `BACKLOG.md` were both already in the repo but never added to that list when first created, only noticed while adding the RAID log entry. Fixed in this session. A small instance of the same underlying pattern as Issue I1 (documentation drifting from actual repo state) — but caught proactively this time, before being committed, rather than after. |
| 2026-06-30 12:03 | Adopted new standing rule: Claude does not have a reliable live clock and cannot accurately timestamp chat responses or infer {{owner}}'s local time — {{owner}} now states the date/time explicitly alongside checklist confirmations and logged events, and Claude uses that rather than approximating "today." | Phase 4 build, once Phase 3.5 documentation closeout (RAID log files committed) is confirmed. | Same as previous entry — RAID log files not yet committed. | Recognized that several existing log entries above (e.g. the 2026-06-28/29/30 entries) were dated by Claude's approximation rather than a date {{owner}} explicitly confirmed — these are left as-is rather than retroactively rewritten, since the approximate dates are still reasonably accurate, but this entry marks the point from which dates are sourced from {{owner}} directly, not inferred. |
| 2026-06-30 12:44 | **Phase 3.5 documentation closeout fully confirmed and verified**: handoff doc v4.0 → v4.1 swap in Project knowledge (screenshot, 12:42), v4.0 archived locally (screenshot, 12:44), `RAID_LOG.md`/`RAID_Log.xlsx`/`build_raid_log.py`/`README.md`/`BACKLOG.md` all committed and pushed (earlier screenshot + `git ls-tree` verification, commit `6e657d0`). No open items remain. | **Phase 4** — execute the tool(s) Claude decides to call, feed results back as observations, loop until goal complete or a stopping condition is hit. Must explicitly handle the 5 proactive-design risks named in the standing rule (zero/multi tool calls, malformed arguments, unrecognized tool names, loop termination, API failures carried into the loop). | None — fully unblocked for Phase 4. SendGrid setup (items D3 in RAID log) still outstanding but only gates Phase 4.5. | The earlier missed-commit incident (staged files, no commit, "Everything up-to-date" on push) was caught and self-corrected by {{owner}} before being reported as done — a good example of the screenshot-verification habit being internalized rather than only enforced by Claude asking for evidence. Not yet formally logged as a RAID Issue, since it was self-caught and resolved within the same exchange; noted here for completeness. |
| 2026-06-30 23:50 | **Phase 4 complete — full agent loop proven end to end.** `agent_loop.py` built and run successfully against the sample folder. Three iterations: (1) Claude called `find_duplicates` + `find_convertible_files` in parallel and both executed for real, returning genuine results; (2) Claude read those results and on its own initiative called `propose_action` with the correct arguments — no scripting, pure LLM decision; (3) Claude returned zero tool calls and a clean human-readable summary, triggering the natural loop-termination condition correctly (not the MAX_ITERATIONS safety cap). Safety boundary confirmed: agent's own closing summary explicitly stated "No files have been modified or deleted — human approval required." All 5 proactive-design risks exercised: Risks 1 and 4 fired on the live run (multi-tool-call in iteration 1, natural zero-call stop in iteration 3); Risks 2, 3, 5 did not trigger on this clean run, as expected — they're there for when things go wrong. | Phase 4 closeout: `agent_loop.py` saved locally, committed, pushed, README and handoff doc updated, BACKLOG updated. Then Phase 4.5 (Resilience & Alerting) — gated on SendGrid setup. | SendGrid account + `SENDGRID_API_KEY` still outstanding (RAID D3, gates Phase 4.5 only). | The loop terminated naturally in 3 iterations out of a possible 6, which is meaningful: it tells us the agent is reasoning efficiently (not burning iterations unnecessarily) and that the MAX_ITERATIONS cap is correctly a safety net, not the normal exit path. Claude's unprompted decision to call `propose_action` in iteration 2 — based purely on reading iteration 1's results — is the clearest live demonstration of the "decide → act → observe → repeat" loop working as an actual agent, not a scripted sequence. Worth highlighting specifically in the Phase 4 README section. |
| 2026-07-01 00:37 | Phase 4 closeout fully confirmed (commit `4f68254`, `git ls-tree` verified, handoff doc v4.2 loaded in Project knowledge, v4.1 archived locally — all screenshots taken at 00:22 and 00:29). New session started. {{owner}} subscribed to Claude paid plan (separate from the Anthropic API credit) to continue the build after free tokens reset didn't happen — both billing systems confirmed independent and operational. External logging suggestion (terminal redirection) reviewed and discussed; decision made to implement **built-in Python logging** inside `agent_loop.py` instead, as a deliberate design choice rather than a terminal workaround — stronger portfolio signal and a natural foundation for Phase 4.5's alerting layer. | Add built-in Python logging to `agent_loop.py` (structured levels: INFO/WARNING/ERROR, timestamped log file per run). Then Phase 4.5 (Resilience & Alerting via SendGrid + state machine). | SendGrid account + `SENDGRID_API_KEY` still outstanding (RAID D3). | Retrospective: the distinction between "capturing output externally" vs "designing the agent to be observable" is a useful framing for the portfolio — the latter reflects intent, the former reflects improvisation. Built-in logging sits naturally under the proactive-design standing rule: observability during a live loop is a reliably-needed property, not an afterthought. Worth adding to the README's Design philosophy section once implemented. |
| 2026-07-01 07:11 | SendGrid setup fully confirmed: account active (free trial to 2026-08-29), Single Sender Verification completed (`nadeemmarshman@gmail.com`, verified, Johannesburg ZAF), API key rotated (old key exposed in uploaded document — replaced immediately, new key stored in Bitwarden + set as `SENDGRID_API_KEY` Windows User environment variable, verified via `echo $env:SENDGRID_API_KEY`). Note: new key also briefly visible in terminal screenshot — flagged for rotation after Phase 4.5 is tested and signed off. All prerequisites for Phase 4.5 now confirmed: `SENDGRID_API_KEY` env var set, verified from address known (`nadeemmarshman@gmail.com`), alert recipients confirmed (`nadeemmarshman@gmail.com` and `narshman@duck.com`), state-machine design already in handoff doc v4.2, ERROR-level logging hook already in `agent_loop.py`. | **Phase 4.5 build** — email alerting (SendGrid) + state-machine-driven restart-and-resume (load-shedding resilience). | None — fully unblocked. | RAID log D3 (SendGrid dependency) can now be marked closed/active. A5 assumption (date/time accuracy) already closed. Good moment to update RAID_LOG.md to reflect SendGrid as active rather than pending. |
| 2026-07-01 17:16 | Phase 4.5 fully closed and verified: `resilience.py` committed (commit `8296fa9`), GitHub audit clean (`git ls-tree` + `git show HEAD:resilience.py` content check confirmed all 5 states, `VALID_TRANSITIONS`, and `AWAITING_HUMAN_APPROVAL` safety guarantee present in committed file). Handoff doc v4.3 loaded in Project knowledge, v4.2 archived locally. PowerShell profile fixed: auto-transcript removed, timestamp prompt added (`[yyyy-MM-dd HH:mm]` visible on every prompt line) — no more session transcript files cluttering `logs/`. Path-first convention for PowerShell commands adopted going forward. | **Phase 5** — human-approval gate before any destructive action (delete, convert) executes. The BA/PM differentiator: governance layer that ties the whole project together. | None. | GitHub sanity check before starting Phase 5 was a good instinct — confirmed clean before proceeding rather than assuming. The PowerShell transcript cleanup also surfaced a useful lesson: side-effects of earlier setup decisions (transcript logging I configured) can create clutter that only becomes visible later. Worth noting as a RAID assumption: no other profile-level side-effects from earlier session configurations. |
| 2026-07-01 21:40 | Handoff doc v4.4 loaded in Project knowledge (21:32), v4.3 archived locally (confirmed screenshot — full archive v1.0–v4.3 visible, 13 versions, clean audit trail). BACKLOG.md saved locally (21:32) with items #3 and #4 added. Pre-Phase-5 quality gate formally documented in handoff doc v4.4: gate condition added to Phase 5 row in plan table, status line, and "How to resume" section. GRC rationale documented: assurance work on capabilities already built sits in the Backlog, not as a new phase — distinguishes delivery controls from quality controls. | **Pre-Phase-5 quality gate** — on-paper test pack design first (what to test, before writing code), then `test_phase3_4_45.py` (Backlog item #3), then enriched alert troubleshooting messages in `resilience.py` (Backlog item #4), then Phase 5. | None. | The archive screenshot (v1.0–v4.3, 13 versions, growing from 9 KB to 40 KB) is a genuine portfolio signal in its own right — verifiable document-control discipline across a real project timeline, not just claimed. Worth calling out explicitly in the Phase 6 README narrative. |
| 2026-07-01 22:47 | **Pre-Phase-5 quality gate fully complete.** Backlog item #4 done: `resilience.py` enriched with 7 named alert functions structured around a 5W incident-management framework (WHAT/WHEN/WHERE/TRIGGERED BY/WHY/STEPS), graduated urgency per error type, `_masked_user_id()` helper added (partial credential masking: first 2 chars shown, remainder `****`), README-reference pattern applied consistently across all 23 command/path references. RAID R5 added (alert email credential exposure risk, Monitoring). Backlog item #3 done: `test_phase3_4_45.py` built — 31 unit tests + 3 integration tests, all 34 passing. Covers: `get_client()` missing key, `validate_arguments()` (missing/unexpected/None arg), `execute_tool_call()` (unrecognised tool, malformed args, runtime error), all 5 state machine scenarios (invalid transition, corrupted JSON, invalid state name, disk write failure, AWAITING_HUMAN_APPROVAL safety guarantee), `_masked_user_id()` masking logic (normal, short, missing env vars), all 7 alert function 5W content checks, masked user ID in all 7 TRIGGERED BY fields, send_alert() missing key, and 3 integration tests (full tool chain, full state-machine cycle with resume, error mid-chain). | **Phase 5** — human-approval gate. Fully unblocked. | None. | The two test failures found and fixed during the run (stub TypeError on Anthropic client construction; multi-line text assertion on OneDrive safety note) are real examples of the test suite doing its job — catching edge cases even in the test code itself, not just in the production code. Worth noting in the RAID log as a small I-series entry or retrospective note. |
| 2026-07-01 23:31 | Handoff doc v4.5 confirmed in Project knowledge (23:06), v4.4 archived (23:06 — archive now holds v1.0–v4.4, 14 versions, full unbroken audit trail). All pre-Phase-5 gate artefacts committed and verified on GitHub (commit `a8fce07`, `git ls-tree` confirmed). Short break taken; resuming same session. | **Phase 5** — human-approval gate before any destructive action (delete, convert) executes. | None. | Clean close-out on the pre-Phase-5 gate — everything confirmed, nothing dangling. Ready to start Phase 5 immediately. |
| 2026-07-02 10:01 | New session. `approval_gate.py` and `test_phase5_live.py` saved locally last session but not yet committed. `RAID_LOG.md` (R6 added) and `AI-Agent-Build-Handoff_v4.5.md` (DR/rollback Decision log entry added) also updated locally. Handoff doc v4.5 re-uploaded to Project knowledge after project rename to "OneDrive-Agent Project". | Run `test_phase5_live.py` live on machine to see the approval gate in action, then add Phase 5 test cases, update remaining docs, commit everything, bump handoff doc. | None. | Clean starting position — nothing dangling from last session, all local files confirmed saved before close. |
| 2026-07-02 13:42 | **Full session summary — major progress across Phase 5, Phase 6, and resume work.** Phase 5 live run completed successfully (invalid input caught, real file deleted on approval, rejection skipped correctly, pre-run snapshot verified with matching MD5 hashes). Test suite extended to 45 tests (40 unit + 5 integration). Phase 6 fully complete: full README written (problem → approach → architecture → governance → testing), architecture diagram built as standalone SVG and rendering inline on the public repo landing page. User roles design decision made and documented (single-user model, Backlog item #5 Actioned). Repo switched from Private to Public at `github.com/nadeemmarshman/onedrive-agent`. Resume bullets drafted — 3 versions (Delivery Lead primary, BA, PM), each with short (resume) and long (cover letter/LinkedIn) form — saved as `Resume_Bullets_OneDrive_Agent.docx` in Career Documents, NOT in the project repo (deliberate scope decision). Handoff doc bumped to v4.7. | Resume and LinkedIn work in Job Search — Resume & LinkedIn Claude project. Phase 7 (Cowork comparison) when ready. | None — project fully closed out at Phase 6. | The architecture diagram rendering live on GitHub on the first attempt, with the README flowing cleanly underneath it, was the clearest single moment of "this is a real portfolio piece now, not just code." Worth noting that the commit history (16 commits, phase-by-phase, each with a descriptive message) is itself a portfolio signal — the build discipline is visible, not just the outcome. |
| 2026-07-02 13:48 | Phases 1–6 fully closed. BACKLOG.md updated with session summary. Resume bullets saved locally. Immediately resuming with Phase 7. | **Phase 7** — compare the hand-built agent (`claude-sonnet-4-6`) against Claude Cowork (`claude-sonnet-5`, released 2026-06-30) on a similar file-organising task; document findings in the README. | Requires a paid Claude plan with Cowork access (Pro/Max/Team/Enterprise). | No gaps from previous session — clean handover into Phase 7. |
| 2026-07-02 22:03 | **Independent Claude Cowork peer review requested and completed.** Cowork reviewed all project files for syntax, internal consistency, and README clarity, without prior grounding beyond the README itself. Found and correctly root-caused a real, critical test isolation bug (Phase 5 integration tests deleting real `sample_data/` fixture files on every run) plus 3 minor documentation drifts (broken `R_SEC_01`→`R5` cross-reference, stale RAID R1 status, incorrect README assumption count). All 4 findings fixed and verified same day — full 45-test suite passing, fixture file hashes confirmed byte-identical before/after. A secondary drift (trailing-newline mismatch causing hash mismatches) was also found and fixed during verification. Full RCA documented in new `REMEDIATION_2026-07-02_cowork-peer-review.md`, cross-referenced from RAID_LOG.md Issue I5. Separately: agent's own model switched from `claude-sonnet-4-6` to `claude-sonnet-5` to align with Claude Chat/Cowork, motivated by the original budget-constrained choice of the free-tier model now being superseded by a justified paid subscription (Decision log item 7). Two new standing rules adopted: (1) confirm before producing files for download, in case something else needs to be folded in; (2) SDLC retrospective labeling applied to the whole project, with a new Phase 8 (UAT/Pilot, not POC — terminology deliberately corrected) added for validating against a real, limited OneDrive subset before full rollout. | Phase 8 (UAT/Pilot) — select a real OneDrive subfolder, update `starting_folder`, run end-to-end with the approval gate. | None. | This was a genuinely strong demonstration of the project's own stated philosophy in action: an independent reviewer caught a real bug that the project's own test suite's internal logic ("skip integration if unit tests fail") was silently masking, root-caused it correctly by actually running the code rather than just reading it, and the fix was verified with hash-level rigour rather than just "tests pass now." Worth highlighting specifically in any interview discussion of this project — it's evidence of genuine quality assurance discipline, not just a claim of it. |
| 2026-07-02 22:31 | Session paused before committing. Nine files finalized and ready for download but **not yet saved locally or committed to GitHub**: `test_phase3_4_45.py`, `resilience.py`, `decision_loop.py`, `agent_loop.py` (bundle 1 — code/bug fixes), `AI-Agent-Build-Handoff_v5.0.md`, `README.md`, `RAID_LOG.md`, `BACKLOG.md`, `REMEDIATION_2026-07-02_cowork-peer-review.md` (bundle 2 — documentation). All verified clean via sanity checks and a full local test run (45/45 passing) before being presented for download. {{owner}} deferring the commit/push and Project knowledge swap to the next session, when fresh, rather than rushing a large batch at day's end — a sound practice given the size of this batch. | Commit and push both bundles; Project knowledge swap (v4.8 → v5.0); then Phase 8 (UAT/Pilot). | None — purely a "pick up where left off" continuation. | Deliberately deferring a large, careful commit to a fresh session rather than rushing it at day's end is itself consistent with the project's own quality discipline — the same instinct that led to catching the Cowork-review findings in the first place. |
| 2026-07-03 11:46 | **Full Cowork remediation bundle confirmed committed, pushed, and documented.** Commit `06d3fc0`: `8 files changed, 377 insertions(+), 43 deletions(-)`, verified via `git ls-tree` — all 8 files present including the new `REMEDIATION_2026-07-02_cowork-peer-review.md`. Handoff doc v5.0 confirmed loaded in Project knowledge (screenshot, 11:37); v4.8 archived locally with the full unbroken history back to v1.0 (screenshot, 11:34). Local project folder timestamps confirm the whole batch was saved together as one coherent unit, not piecemeal. | **Phase 8 — UAT/Pilot.** Select a real, contained OneDrive subfolder, update `starting_folder` in `agent_loop.py` and `test_phase5_live.py`, run end-to-end with the approval gate active. | None. | Clean, fully verified close-out on the remediation work — nothing dangling, nothing unconfirmed. Ready to move into the project's final planned phase. |
| 2026-07-03 12:12 | **Sanity-check process improvement.** Noticed that this session's sanity checks had produced repeated false positives, all traceable to three recurring root causes: (1) line-wrapped markdown table cells causing exact-phrase misses, (2) multiple same-shaped tables in one file (e.g. `BACKLOG.md`'s Update Log vs Daily Scrum Log) being conflated by naive row-counting, (3) exact-phrase brittleness where content was correct but phrased slightly differently than the check expected. Built a small reusable checking helper (kept outside the project repo, at `/home/claude/sanity_check_helper/check.py` in the working sandbox — not a project artefact, not committed) with whitespace-normalised phrase matching and section-scoped row extraction, and verified it against today's exact false positives to confirm they're resolved. Applied forward-only, not retroactively — prior flagged items were already manually re-verified as correct at the time, so re-running old checks with the new method would confirm the same conclusion at zero added value. | Apply this checking method for all future sanity checks in this project. | None. | This is itself a small, real example of the project's own proactive-design philosophy applied to Claude's own working process, not just the code: noticing a recurring failure pattern and fixing the root cause once, rather than re-diagnosing the same false positive in every future check. {{owner}} extended this expectation beyond this project — Claude should proactively recognise this class of pattern (a root cause rediscovered across multiple scenarios of the same process) across all projects and conversations, not only when prompted. |
| 2026-07-04 18:24 | **Resolved a cross-project instruction spillover.** The Job Search — Resume & LinkedIn Claude project had suggested four README/repo "refinements" to this project, written without full context of this project's actual code structure. Reviewed all four here rather than executing them blindly: Refinement 1 (demo capture via `agent_loop.py`) was rejected outright — it incorrectly assumed `agent_loop.py` invokes the approval gate, when only `test_phase5_live.py` actually does; running it as instructed would not have produced the claimed demo and could have caused confusion about which script does what. Refinement 2 (relocate "Project location and key paths" section below the build narrative) completed cleanly — verified no code references the section by position first. Refinement 3 (source or soften the SWE-bench Pro benchmark claim) completed — figure independently re-confirmed via live search against Anthropic's own June 30, 2026 announcement and System Card, then cited directly in the README rather than left as an unsourced claim. Refinement 4 (move `RAID_Log.xlsx` and the remediation report into a new `docs/` folder) completed — but the original instructions' own reference-check missed the handoff document entirely; checked it explicitly, confirmed its only mentions are historical version-history entries that correctly stay unedited (the same principle as not retroactively "fixing" old sanity checks). All commits pushed cleanly; one incidental merge required (`LICENSE` file added directly on GitHub via the web UI, unrelated to this work, merged without conflict). | **Phase 8 — UAT/Pilot.** Select a real OneDrive subfolder, update `starting_folder`, run end-to-end with the approval gate. | None. | A good demonstration of why cross-project instructions shouldn't be executed at face value: an assistant without this project's full code context produced one genuinely flawed instruction (Refinement 1) and one incomplete reference check (Refinement 4) alongside two solid suggestions. Reviewing everything through this project's own verification discipline caught both issues before any file was touched incorrectly. |
| 2026-07-05 | **Phase 8 Definition-of-Ready gate completed.** On-paper test-design deliverables produced (`TEST_STRATEGY.md`, `TEST_BED_AND_CASES.md`), closing Backlog item #1. Two pre-pilot tests added (`test_phase8_pre_pilot.py`): loop-termination coverage (gap G1) and a fixture-integrity guard (I5 class). Test-isolation fix applied to `test_phase3_4_45.py` — cleared `SENDGRID_API_KEY` so the suite can't send real alerts (Issue I6). Missing fixture (`notes_copy.txt`) restored from HEAD in the local working tree. Full suite re-run green: 18 / 45 / 4, no emails, fixtures byte-identical, safety cap proven at 6 iterations. Commit `26c7ad2` landed the code/docs; BACKLOG #1-actioned and RAID I6/I7 were corrected in a follow-up commit. | Bump handoff to v6.1; run the Phase 8 pilot per `TEST_BED_AND_CASES.md`. | Issue I7 (SendGrid key exposed in the REPL) — key rotation to be confirmed before the live pilot. | The DoR gate earned its keep: it caught three latent defects before any real data was touched — a fixture drifted from HEAD (residue of I5), the suite silently emailing the live recipient list on machines with a key set (I6), and loop-termination logic proven only by a one-off live run (G1). Two of the three share one root pattern — tests with real, environment-dependent side effects — motivating the new test-suite hermeticity standing rule. |

---

## Footnotes (methodology reference)

1. **Product Backlog** — standard Scrum artifact: an ordered list of
   everything that might be needed, not yet committed to a Sprint. Used
   here for ideas/scope questions raised mid-build, before being actioned
   into the handoff document.
2. In real Scrum, Backlog Items are typically refined into a Sprint
   Backlog and "Done" per a Definition of Done. This project substitutes
   "Actioned → reflected in handoff doc/README" as its lightweight
   equivalent, given there is no formal Sprint cycle.
3. **Daily Scrum** is the correct Scrum Guide term (commonly nicknamed
   "stand-up" in industry, but "Daily Scrum" is the formal name used here
   deliberately).
4. The Scrum Guide's three Daily Scrum questions are adapted here as
   "what did we complete," "what's planned," and "blockers" — functionally
   equivalent to "what I did yesterday / what I'll do today / impediments."
5. A **Sprint Retrospective** is formally an end-of-Sprint ceremony focused
   on process improvement, requiring a fixed Sprint boundary. This project
   is phase-driven with irregular session cadence, not time-boxed Sprints,
   so a separate Retrospective ceremony would misuse the term. Instead,
   retrospective-style reflection is folded into each Daily Scrum entry —
   an explicit, intentional adaptation, not a terminology error.
6. This column is the project's adapted substitute for a Sprint
   Retrospective's "what went well / what didn't / what will we change"
   reflection — scoped to a single working session rather than a Sprint.
