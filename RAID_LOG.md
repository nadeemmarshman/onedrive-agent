# OneDrive Agent — RAID Log

**Purpose:** Risks, Assumptions, Issues, and Dependencies tracked for this
project, as a standard PM control artifact — separate from `BACKLOG.md`
(which tracks Agile/Scrum-style delivery and process) and the handoff
document (which tracks agreed project state and standing rules). RAID
tracks *what could go wrong, what we're assuming, what has gone wrong, and
what we depend on* — complementary to, not a duplicate of, the other two.

Status values: **Open** (active, not yet resolved/mitigated) | **Closed**
(resolved, mitigated, or no longer applicable, with resolution noted) |
**Monitoring** (not currently active, but being watched)

---

## Risks

Things that **might** happen, with a negative impact if they do.

| # | Raised | Risk | Likelihood | Impact | Mitigation | Status |
|---|---|---|---|---|---|---|
| R1 | 2026-06-28 | Load shedding / power loss interrupts the agent mid-loop, losing progress or leaving an action in an unsafe partial state. | High (recurring, locale-specific operational reality in South Africa) | Medium-High — could lose work, or worse, resume incorrectly into a destructive action without approval. | Phase 4.5: explicit state-machine design with persisted state; `AWAITING_HUMAN_APPROVAL` must never be silently skipped on resume. Implemented in `resilience.py` (2026-07-01): 5-state machine, `agent_state.json` persistence, `check_for_resume()` safety guarantee. Verified by unit test `test_unit_check_for_resume_awaiting_human_approval_safety`. **Reopened 2026-07-19 (see I16): the prior "Closed — implemented and verified" status rested only on that isolated unit test, not on the live path.** RB-13's investigation found the live invocation path (`agent_loop.py`, `approval_gate.py`, `test_phase5_live.py`) never actually calls `save_state()`/`check_for_resume()` — the mitigation exists in code but was never wired into anything runnable. The practical safety outcome is still proven (I16: an unplanned kill mid-approval does not result in unapproved execution), but via "always restart clean," not the state-machine resume this risk originally named. Live wiring deferred post-pilot (Backlog #10). | Monitoring — practical safety outcome proven (I16); the specific state-machine-resume mitigation named above is not live, re-scoped 2026-07-19 |
| R2 | 2026-06-30 | Verbal/status-only confirmation between sessions could drift from actual file/repo/Project-knowledge state, since there is no automatic way to verify "done" claims. | Medium (already materialized once — see Issue I1) | Medium — could lead to working from a stale or incorrect document/codebase without realizing it. | Screenshot-verification standing rule adopted (handoff doc v4.0) — checklist items require visual evidence, not verbal confirmation alone. | Monitoring — mitigation in place, watching for recurrence |
| R3 | 2026-06-28 | Real API/network failures (rate limits, connectivity, insufficient credit) during a live Anthropic API call. | Medium (normal for any real API integration) | Low-Medium — would crash the script ungracefully if unhandled. | Specific exception handling implemented in `decision_loop.py` (Phase 3, v3.2) for auth, rate limit, connection, and general API errors. | Closed — mitigated and verified (simulated test of each branch) |
| R4 | 2026-06-28 | An LLM-driven decision loop could return zero, multiple, or unrecognized tool calls, or malformed arguments, none of which a "happy path only" execution loop would handle correctly. | High (normal LLM agent behavior, already observed once — Claude returned two tool calls in the same turn during Phase 3 testing) | Medium — could silently skip work, crash, or call an undefined function. | Implemented in `agent_loop.py` (Phase 4): zero/multi tool calls collected via list comprehension over all `tool_use` blocks; malformed arguments validated against function signatures before execution; unrecognized tool names checked before lookup. Verified via stubbed unit tests, then directly exercised in a live run (2 tool calls in iteration 1, 0 in iteration 3, correctly handled both times). | Closed — implemented and verified, both by unit test and live run |
| R5 | 2026-07-01 | Alert emails contain partially masked user credentials (username@hostname) and operational detail (file paths, error specifics). If intercepted, forwarded accidentally, or landing in an unsecured inbox, this information could assist a threat actor in identifying valid accounts and machine names. | Low-Medium — personal portfolio project, single-machine, single-user; not a corporate network. | Low-Medium — masked credentials reduce but do not eliminate exposure risk. Operational detail in the email body (paths, error text) remains readable. | Partial masking implemented in `_masked_user_id()` in `resilience.py`: first 2 characters of username and hostname shown, remainder replaced with `****` (mirrors industry-standard partial-masking pattern). Alert recipients limited to owner-controlled addresses. Residual risk accepted: users must ensure alert inboxes are secured and alerts are not forwarded to uncontrolled distribution lists. | Monitoring — mitigation in place, residual risk accepted and documented |
| R6 | 2026-07-01 | Phase 5 introduces destructive action execution (file deletion, format conversion). If an action executes incorrectly (wrong file, unexpected behaviour), there is no agent-managed recovery path. | Low — OneDrive's built-in recycle bin and version history (30–180 days depending on plan) already provides a recovery mechanism at the platform level. | Medium — incorrect deletion of a real file would require manual recovery via OneDrive recycle bin or version history. | Two-layer mitigation: (1) Platform-level: OneDrive recycle bin and version history provide adequate recovery for this project's scope — building a redundant backup mechanism was considered (Option B: full file copy before execution) and explicitly rejected as overkill given this existing platform capability. (2) Agent-level: a pre-run manifest snapshot (`pre_run_snapshot.json`) is written before any Phase 5 action executes — a JSON record of every file's path, size, and hash at that point in time, providing an audit trail of what existed before the run without duplicating file data. Decision logged in handoff doc Decision log. | Monitoring — platform-level mitigation (OneDrive) adequate; manifest snapshot adds agent-level audit trail |

---

## Assumptions

Things taken as true without proof, which could turn out to be wrong.

| # | Raised | Assumption | Risk if wrong | Status |
|---|---|---|---|---|
| A1 | 2026-06-30 | A successful `git commit`/`git push` (no error output) means the intended, correct file content was committed. | **Proven false** — see Issue I1. A commit can succeed with completely wrong content (e.g. an accidental overwrite) and show no error at all. | Closed — disproven; replaced by the practice of checking commit insertion/deletion stats and, where it matters, verifying actual committed content (e.g. `git show HEAD:file`). |
| A2 | 2026-06-26 | SendGrid's free tier (100 emails/day) will be sufficient for this project's alerting volume. | Low risk if wrong — alert volume for a personal portfolio project should be very low; would only become a problem under a failure loop sending repeated alerts. | Monitoring |
| A3 | 2026-06-26 | The $6 / minimum Anthropic API credit balance will be sufficient to complete Phases 3 through 7 of testing and development. | Low-Medium — if wrong, would require topping up credit; not a functional blocker, just a cost one. | Monitoring |
| A4 | 2026-06-26 | Python 3.14 (a very new release) will continue to have adequate third-party package support (prebuilt wheels) for the libraries this project needs. | Medium if wrong — already caused one build failure (`tokenizers`/Rust compiler issue), resolved by updating pip; could recur with a different package. | Monitoring |
| A5 | 2026-06-30 | Claude's own approximate dating of log entries (based on system context "today") would be accurate enough for project record-keeping purposes. | Low-Medium — not disproven as factually wrong, but identified as an unreliable practice for a portfolio artifact valuing accuracy; could understate precision if ever scrutinized closely. | Closed — addressed proactively before becoming an Issue, by adopting an explicit convention (handoff doc v4.0) requiring {{owner}} to state dates/times directly rather than Claude approximating them. |
| A6 | 2026-07-02 | Results produced by this agent (Phases 3–5, using `claude-sonnet-4-6`) and by Claude Cowork (Phase 7, using `claude-sonnet-5`) are representative of those model versions at the time of testing. Re-running against different model versions may produce different findings, reasoning, or proposals — particularly as models improve. Note: Cowork ran on Sonnet 5 (released 2026-06-30, the day before this comparison), which is a capability upgrade over Sonnet 4.6 with the largest gains in agentic tasks — meaning Cowork had a modest capability advantage in this comparison. | Low — findings on the controlled `sample_data/` test set are deterministic (file content doesn't change); reasoning and natural-language output may vary by model version. | Monitoring — model versions recorded in README Phase 7 section and in `decision_loop.py`/`agent_loop.py` source code for traceability. **Update 2026-07-02:** the agent's own model was subsequently switched from `claude-sonnet-4-6` to `claude-sonnet-5` (see handoff doc Decision log, item 7), removing the version mismatch this assumption originally flagged. The Phase 7 comparison record above remains historically accurate as a snapshot of that specific run; going forward, all three surfaces (Claude Chat, Claude Cowork, this agent's API calls) run on Sonnet 5. |
| A7 | 2026-07-08 | OneDrive root folders `My Music`, `My Pictures`, `My Videos` show account-level sync-permission errors (screenshots on file). An OneDrive account issue, not an agent defect — out of this project's lane. Excluded from all test beds. | Low — the access-denied class is covered synthetically by RB-06, so pilot coverage is not lost. See `TEST_BED_AND_CASES.md` v2.0 §8.5. | Open |
| A8 | 2026-07-08 | `<family-member-3>` is a shared OneDrive folder with different permission/sync semantics. Excluded from all test beds; shared/multi-owner behaviour logged as a future consideration, not a pilot case. | Low — no pilot case depends on it. See `TEST_BED_AND_CASES.md` v2.0 §8.5. | Open |
| A9 | 2026-07-08 | Live Documents tree fully hydrated — online-only placeholders no longer occur naturally on this machine. RB-10's placeholder must be deliberately created (`attrib +U -P`). | Medium if ignored — pilot sign-off could overclaim placeholder coverage beyond the single exploratory RB-10 case. Relates to gap G3; see `TEST_BED_AND_CASES.md` v2.0 §8.5. | Open |
| A10 | 2026-07-15 | Users may assume the agent detects visually-similar images or near-duplicate text ("these photos look the same"); it detects exact MD5 (Message Digest 5) content duplicates only — fuzzy/similarity matching is deliberately out of scope (see handoff Decision log, 2026-07-15). | Medium if unmanaged — an expectation gap at rollout; pilot sign-off wording must not overclaim image or near-duplicate coverage. | Closed — checked at sign-off: `PILOT_SIGNOFF_SUMMARY.md` §4 explicitly states exact-hash-only coverage and disclaims image/near-duplicate detection; does not overclaim. Signed off by {{owner}}, 2026-07-20 05:46. |
| A11 | 2026-07-30 | **Live-run phase.** A user's explicit authorization is sufficient for the assistant to execute a permanent, unrecoverable file deletion on request. | **Proven false this phase** — during the live run against the real `OneDrive\Pictures\Camera Roll` folder, {{owner}} asked for the two identified duplicate files to be deleted outright; declined, as permanent deletion is a standing safety boundary for this assistant regardless of user authorization, not a project-specific rule. Full paths and a manual PowerShell/File-Explorer command were given instead. Led directly to a workflow change (quarantine-move instead of direct delete — see live-run Decision log). | Closed — disproven and resolved 2026-07-30; workflow changed to quarantine-based move (`quarantine_camera_roll_dedup.py`) so the final delete remains a deliberate, separate, human-initiated step |
---

## Issues

Things that **have already happened** and required resolution.

| # | Raised | Issue | Root cause | Resolution | Status |
|---|---|---|---|---|---|
| I1 | 2026-06-30 | `README.md` and `BACKLOG.md` were overwritten with one-line placeholder text (via `echo "..." > file`) instead of containing their real, previously-generated content, and this was committed and pushed to GitHub before being caught. | Combination of **Risk R2** (status drift between sessions) and **Assumption A1** (wrongly trusting a clean commit/push as proof of correct content) — the actual files on disk were placeholder versions at the time of commit, not the real ones generated earlier in the session. | Caught via the newly-adopted screenshot-verification rule, by noticing the commit's `84 deletions(-)` stat didn't match what should have been a pure-addition commit. Real files re-downloaded, re-saved locally, re-committed (`198 insertions(+), 2 deletions(-)` — correct signature this time), and verified directly via `git show HEAD:README.md` confirming the actual committed content, not just commit stats. | Closed — resolved 2026-06-30, verified by content inspection, not just commit success |
| I2 | 2026-06-26 | `pip install anthropic` failed during Phase 3 setup: the `tokenizers` dependency attempted to compile from Rust source, and no Rust compiler was present on the machine. | Python 3.14 is a very new release; a prebuilt wheel for `tokenizers` may not yet have been available for it at the time, causing pip to fall back to a source build. | Updated pip first (`python -m pip install --upgrade pip`), after which a prebuilt wheel installed cleanly without needing Rust. | Closed — resolved 2026-06-26 |
| I3 | 2026-06-30 | Confusion over which handoff document version (v3.0 vs v3.2) was actually loaded in Project knowledge, due to an intermediate swap that wasn't explicitly confirmed back in chat. | Same root cause as R2/A1 — reliance on verbal/remembered status rather than verified evidence. | Clarified via direct question and screenshot; correct version (v4.0) confirmed loaded via Project knowledge files panel screenshot. | Closed — resolved 2026-06-29/30 |
| I4 | 2026-07-02 | Phase 5 introduced destructive action execution for the first time — risk of acting on the wrong file or losing progress if the run is interrupted mid-execution with no audit trail. | Risk R6 (DR/rollback): no agent-managed record of what existed before the run. | Pre-run manifest snapshot (`pre_run_snapshot.json`) implemented as a hard precondition in `approval_gate.py`: if the snapshot cannot be written, all execution is blocked ("no snapshot, no actions"). Snapshot records path, size, MD5 hash, and modification timestamp for every affected file before any action executes. Proven in a live run (2026-07-02): snapshot written at `2026-07-02T08:02:47 UTC`, two duplicate `.txt` files correctly recorded with matching MD5 hashes confirming they were genuine duplicates. OneDrive recycle bin accepted as the primary file-recovery mechanism. | Closed — implemented and verified in live run |
| I5 | 2026-07-02 | `test_phase3_4_45.py`'s two Phase 5 integration tests (`test_integration_full_phase5_approve_all` / `_reject_all`) scanned and operated on the **real** `sample_data/` fixture folder rather than an isolated copy. Since `_execute_delete()` is not mocked in these tests, "approve all" genuinely deleted real fixture files on every test run — a self-inflicted, recurring bug that caused `sample_data/` to silently drift out of sync with what `test_phase2.py` expects (2 unit test failures: file count and duplicate-group size), which in turn caused the integration layer to be skipped entirely under the suite's own "skip integration if unit tests fail" rule. | Test isolation gap — no test in the suite created an independent copy of test fixtures before performing destructive operations on them. | Caught by an independent Claude Cowork peer review (2026-07-02) that actually ran the test suite rather than only reading the code, and diagnosed the exact root cause. Fixed by rebuilding both integration tests to copy `sample_data/` into a temporary directory (`tempfile.mkdtemp()`) via `shutil.copytree()`, operate entirely within that temp copy, and clean it up in a `finally` block — the real fixture folder is never touched. Verified: full 45-test suite re-run with fixture file hashes (MD5) confirmed byte-identical before and after the run. A secondary, related drift was also found and fixed during verification: the buggy test's file-recreation used `Path.write_text()` without a trailing newline, while the original fixture file (created via a shell `echo` command in Phase 1) has one — producing files with matching content but different bytes/hashes. Fixed by recreating the duplicate files as exact byte-for-byte copies (`shutil.copy`) of the original rather than retyping the content. | Closed — fixed and verified 2026-07-02 |
| I6 | 2026-07-05 | The automated test suite (`test_phase3_4_45.py`) sent **real SendGrid alert emails** to the live recipient list during the Phase 8 Definition-of-Ready run. The Phase 5 snapshot-write-failure negative tests exercise the real `alert_on_error → send_alert` path, and `send_alert()` only short-circuits when `SENDGRID_API_KEY` is unset — so on a machine where the key **is** set (the dev box), those tests emailed both recipients on every run. Four messages were received; the mangled sender address on two of them (`nadeemmarshman_at_gmail.com_narshman@duck.com`) was a **DuckDuckGo Email Protection relay rewrite** on the copy forwarded via the `@duck.com` address — confirmed by the `Duck-Original-From` header — **not** an agent bug. | Test-isolation gap in the same family as **Issue I5**: a code path with a real external side effect (network email) was neither stubbed nor key-cleared, unlike the alert-*content* tests which intercept `send_alert` and set a fake key. Environment-dependent, so it stayed invisible on machines without a live key set (e.g. the sandbox where the suite was first verified). | Cleared `SENDGRID_API_KEY` from the environment at the top of `test_phase3_4_45.py` (`os.environ.pop("SENDGRID_API_KEY", None)`), so no code path in the suite can reach the real send. The real `send_alert` is preserved so `test_unit_send_alert_missing_sendgrid_key` still validates its graceful-degradation path, and the alert-content tests are unaffected (they install their own capture). Verified 2026-07-05: full 45-test suite re-run green with the guard, and the snapshot-failure tests now log "SENDGRID_API_KEY … not set" instead of sending. Motivates a new test-suite **hermeticity** standing rule (no real filesystem, network, or credentials in automated tests). | Closed — fixed and verified 2026-07-05 |
| I7 | 2026-07-05 | The live `SENDGRID_API_KEY` value was exposed in an assistant chat transcript: `os.environ.pop("SENDGRID_API_KEY", None)` was run **interactively in the Python REPL**, which echoed the popped secret as the expression's return value. | The interactive REPL prints the value of the last evaluated expression; `os.environ.pop()` returns the removed value. A line written for a module (where the return value is discarded) leaked the secret when pasted into a live prompt. Realisation of **Risk R5** (credential exposure), extended to the SendGrid API key itself. | Key treated as compromised: rotation steps provided (revoke old key in SendGrid; issue a new **Mail Send-only**, least-privilege key; set `SENDGRID_API_KEY` via a masked `Read-Host -AsSecureString` / `Get-Credential` prompt so the value never enters shell history; restart the terminal). Practice note adopted: never let a secret be the last evaluated expression in a REPL (bind to `_` or wrap it). | Closed — key rotated and confirmed 2026-07-05 (see Dependency D3) |
| I8 | 2026-07-14 | Stray Git repository found initialised in the Windows user-profile folder (`<USER_HOME>`) — a `git init` run from the wrong directory during an assisted session outside this Project. Zero commits, but it offered to track the entire profile (registry, browser data, OneDrive tree, personal documents) — one `git add .` from staging personal data. | Ungoverned-session side effects (same family as I1 and the A7–A9 drift). Caught by a `git status` run from the wrong folder reporting a repository instead of erroring. | Defused 2026-07-14 09:46 — `.git` deleted from profile root, verified gone (`Test-Path` False; `git status` = "not a git repository"). No commits, no data exposed. Practice reaffirmed: never `git add .`; always work from a terminal opened in the repo. | Closed |
| I9 | 2026-07-15 | The committed §8.3 manifest-export command (`Export-Csv` without `-Encoding UTF8`) silently corrupts unicode filenames in the CSV on Windows PowerShell 5.1 — caught at CP1 via the unicode-filename test case appearing as literal `?` characters in the CSV data. | Windows PowerShell 5.1's default `Export-Csv` encoding is not UTF-8; the spec command was written and committed without an explicit encoding, and no test case had exercised a unicode filename through the export until CP1. | CP1 manifest re-exported with `-Encoding UTF8` and verified clean (2026-07-15). Spec corrected in `TEST_BED_AND_CASES.md` v2.1 (committed and live-verified 2026-07-17): `-Encoding UTF8` on all three checkpoint exports, plus the previously-missing hash-grouping command and a real expected-output example. | Closed — fixed at CP1 2026-07-15; spec correction committed 2026-07-17 |
| I10 | 2026-07-19 | Live-run delete of `locked_readonly.txt` (one of 5 approved deletions) failed with a Windows access-denied error (WinError 5) instead of succeeding. | The file is read-only/locked at the OS level, so Windows correctly blocked the delete — this is expected behaviour for test case RB-05 (read-only file handling), not a defect in the agent. | No fix needed. Alert email fired correctly on the failure (confirmed by screenshot). Parked follow-up (not urgent): check `locked_readonly.txt`'s file properties to understand why the earlier dry-run succeeded on this same file but the live run failed. | Closed — working as designed, 2026-07-19 |
| I11 | 2026-07-19 | The CP3 manifest-export command was copied from §8.3 as written, but the filename in that command was still hardcoded to `CP1_manifest.csv` — this overwrote the original CP1 baseline file with post-run data instead of producing a separate CP3 file. | `TEST_BED_AND_CASES.md` §8.3 gives one reusable export command without per-checkpoint filename substitution, so copying it verbatim at CP3 reused the CP1 filename. | Two parts. (1) Spec fix: `TEST_BED_AND_CASES.md` bumped to v2.2 (2026-07-19) — §8.3 now gives three explicit, individually-labeled export commands (CP1/CP2/CP3), each with its own filename spelled out, replacing the single hardcoded example + prose "repeat" instruction that caused this. (2) Data: the original CP1_manifest.csv file itself was **not** recovered — it was overwritten, not backed up. CP2 (independently proven identical to true CP1 on 2026-07-17) remains the permanent baseline stand-in for this pilot's CP3 comparison; the pilot's evidence trail is sound but relies on CP2-as-CP1 going forward, not a restored original CP1 file. | Closed — spec corrected (`TEST_BED_AND_CASES.md` v2.2, 2026-07-19); CP2 stands permanently as this pilot's CP1-equivalent baseline |
| I12 | 2026-07-19 | RB-06 (ACL-denied delete execution) was left unexercised by both the dry run (2026-07-15) and the live run (2026-07-19) — the model's keep-choice for Group A landed on `denied.txt` both times, so it was never proposed for deletion. Run as the targeted fallback per `TEST_BED_AND_CASES.md` v2.3 §5 ("RB-06 fallback procedure — running it standalone"), invoked interactively by {{owner}} so the approval is a genuine human keystroke, not scripted input. | Not a defect — a coverage gap from the model's non-deterministic keep-choice, already anticipated and logged (Decision log, 2026-07-15) with this fallback as the agreed remedy. | **RB-06 now proven.** Approved delete of `denied.txt` failed with `[WinError 5] Access is denied`, caught by `_execute_delete`'s `PermissionError` handler, logged, and alerted (real SendGrid email confirmed on file — same DuckDuckGo relay header rewrite as I7, not a new issue). Run summary matched the documented expected outcome exactly: `{'succeeded': 0, 'failed': 1, 'skipped': 0, 'status': 'complete'}`. Post-run verification: `denied.txt` still exists and its MD5 (`F807552F27F3CF51C0313FBCAE2E5DFB`) is unchanged from the pre-run value — no partial or corrupt write, delete correctly blocked rather than bypassed. `pre_run_snapshot.json` from Session 2b (2026-07-19 live run) was backed up to `C:\OneDrive-Agent_Audit\pre_run_snapshot_session2b_20260719.json` before this run overwrote it. *Footnote:* two earlier attempts to script the approval via a piped `"y"` failed on a PowerShell→Python stdin BOM artifact (`'﻿y'` rejected as invalid input) — a tooling issue in that invocation method, not a defect in `approval_gate.py`; resolved by running the command interactively instead, which also strengthens this as genuine human-approval evidence. Not logged as its own RAID item — noted here only. | Closed — RB-06 proven, 2026-07-19 |
| I13 | 2026-07-19 | RB-09 (broken-snapshot precondition, "no snapshot, no actions") had never been exercised — unlike RB-06, it isn't something a normal pilot run could trigger naturally, so it requires deliberately breaking the snapshot write first. Run as a standalone targeted exercise per `TEST_BED_AND_CASES.md` v2.4 §5 ("RB-09 fallback procedure — running it standalone"), per the agreed Option B plan (separate from the main pilot run). | Not a defect — an always-separate exercise by design, already logged as outstanding in the handoff's agreed RB-06→RB-09→RB-08/10→RB-11/13 sequencing (2026-07-19). | **RB-09 now proven.** `pre_run_snapshot.json` made unwritable via `icacls ... /deny "$env:USERNAME:(WD)"` (Write Data); the subsequent run hit `PermissionError` inside `write_snapshot()`, was caught, logged as `BLOCKED`, and alerted — `run_approval_gate()` returned `{'status': 'blocked', 'reason': 'Snapshot write failed -- no actions executed.', 'succeeded': 0, 'failed': 0, 'skipped': 0}` exactly as documented. **No approval prompt was ever shown** — execution blocked before `present_proposals()` was reached, the strongest form of "no snapshot, no actions." Post-run: `pre_run_snapshot.json`'s pre-existing content (RB-06's snapshot data) survived completely untouched — a failed `open(path, "w")` does not truncate. Deny ACL removed as teardown, permissions confirmed back to baseline. **Alert independently verified via a live Gmail search** (first real use of the Gmail connector, permission scoped by {{owner}} to SendGrid/OneDrive Agent alert emails only) rather than a manual screenshot — subject `[OneDrive Agent] Execution blocked — snapshot write failed`, delivered within 1 second of the run, full body content matched the terminal `ALERT:` output verbatim. Found in passing: `_execute_delete`'s "Action failed — permission denied" alert is subject-line identical regardless of which file failed (RB-05 and RB-06 alerts collide on subject alone) — noted in `TEST_BED_AND_CASES.md` v2.4 for future alert verification, not logged as a separate issue. | Closed — RB-09 proven, 2026-07-19 |
| I14 | 2026-07-19 | RB-08 (OneDrive recycle-bin recovery — the RAID R6 recovery mechanism) required a run inside real, synced OneDrive rather than the isolated main bed. Run per the RB-08 brief against a secondary bed at `~\OneDrive\OneDrive-Agent_TestBed_OD` (`r.txt` + `r_copy.txt`, identical MD5 `ADBB724B0AF40228854B33842E4042FB`). **Precondition correction:** the brief claimed the secondary bed was already built — it did not exist on this machine and was rebuilt from the brief's own setup block before the run. | Not a defect — a planned OneDrive-specific pilot case, third in the agreed RB-06→RB-09→RB-08/10→RB-11/13 sequence. | **RB-08 PASS.** Run twice by {{owner}} interactively (first ~17:38 local; the evidentiary second run at 17:49:30 local, machine-timestamped by its `pre_run_snapshot.json`). Deterministic keeper confirmed as predicted (no LLM in this chain; `propose_action()` keeps first-in-scan-order `r.txt`): exactly 1 proposal — delete `r_copy.txt` — approved with a genuine human keystroke; execution `1 succeeded, 0 failed, 0 skipped`. `r_copy.txt` then confirmed in **OneDrive's own** Recycle Bin (web UI), restored from there, and re-materialized locally at 17:50:43 (file CreationTime — the restore's local-sync moment); post-restore hash identical to pre-delete. Four screenshots validated by the parallel Claude Project workstream: before / in-bin / after-restore / bin-empty-after-restore. **Reviewer note:** OneDrive's web UI can be slow to refresh a restored file's "Modified" metadata display — a cosmetic caching quirk, not an agent or restore defect; the file's presence, hash, and the bin no longer listing it are the real proof. RB-06's prior `pre_run_snapshot.json` was backed up to `C:\OneDrive-Agent_Audit\pre_run_snapshot_rb06_20260719.json` before this run overwrote it. | Closed — RB-08 PASS, R6 recovery mechanism proven, 2026-07-19 |
| I15 | 2026-07-19 | RB-10 (online-only / placeholder file — EXPLORATORY, gap G3): observe what the agent actually does when scanning a OneDrive placeholder whose content lives only in the cloud. Run autonomously by Claude Code against the secondary bed (`r.txt` deliberately made online-only via `attrib +U -P`, verified `A O U` by metadata-only check immediately before the run; `r_copy.txt` fully local). Observation-only — the approval gate was deliberately not invoked, no actions executed. **Evidence note for reviewers:** unlike RB-06/08/09 there is no approval prompt or UI state to screenshot — the raw console output of the scan/propose chain is the evidence record for this case, plus before/after `attrib` checks. | Not a defect — a planned exploratory case (the one genuinely unhandled risk class in the rollout target), fourth item in the agreed sequence. | **Observed outcome: behaviour (2) of the three candidates — reading triggers hydration.** `scan_folder()` reads metadata only and reported the placeholder's logical size (11 bytes) correctly *without* hydrating it; `find_duplicates()` → `_hash_file()` → `open(rb)` caused Windows' cloud filter driver to transparently download the real content mid-read — hash correct, both files correctly grouped as one duplicate group, correct single proposal produced. **The feared silent-failure mode did NOT occur:** no 0-byte read, no false grouping of unrelated placeholders, no error; `tools.py`'s `except (OSError, PermissionError)` skip path never triggered. Post-run `attrib` confirmed `r.txt` back to plain `A` (hydrated). Corroborated at the Windows level pre-test: a plain `Get-Content` also hydrates — this is filter-driver behaviour, not agent-specific. **Trade-off:** correctness unaffected, but scanning a folder of placeholders silently downloads all of them — bandwidth/disk cost, and it silently undoes the user's "free up space" choice; material at Documents-rollout scale. Handle-vs-accept decision flagged to {{owner}} as Backlog #9. Run 2026-07-19 ≈18:06 local (machine-clocked 18:07:55 immediately after). *Incidental note:* the RB-08 brief's RB-10 setup command was mangled (two commands merged into one line); executed in corrected two-line form. | Closed — observed and documented; decision pending as Backlog #9 (handle vs. accept), sign-off wording must state the hydration behaviour |
| I16 | 2026-07-19 | RB-13 ("interrupted run → resume safety," the crown-jewel test) could not be exercised as originally specified — the live invocation path (`agent_loop.py`, `approval_gate.py`, `test_phase5_live.py`) never calls `resilience.py`'s `save_state()`/`check_for_resume()`; the only callers repo-wide are `test_phase3_4_45.py`'s unit tests, which exercise those functions in isolation. Confirmed empirically, not just by grep: `agent_state.json` did not exist anywhere on disk before this investigation. | **Root cause: a structural Phase 4.5 ↔ Phase 5 integration gap** — `resilience.py`'s state machine (Phase 4.5) was never wired into `agent_loop.py` (Phase 4) or `approval_gate.py` (Phase 5) in the live execution path. **Not** the v8.3 "Option (b) vs (c)" live-run-invocation decision (that was about which folder `test_phase5_live.py` targets, unrelated to state persistence) — an initial mischaracterization, corrected before this entry was logged. | RB-13 redefined to test the behavioural property that actually matters — does an unplanned crash ever result in unapproved execution — rather than the literal state-file-resume mechanism. **Verified via a genuine unplanned process kill** (real background process, real PID, terminated mid-approval by an actual session teardown event) — not a scripted or manually-timed interruption. Sequence: live run reached 3 real proposals (2 deletes + 1 convert) and the genuine `Approve? (y/n):` prompt, `pre_run_snapshot.json` confirmed written; process died unexpectedly mid-approval; post-kill, `agent_state.json` absent and both proposed-delete targets confirmed untouched; restarted, re-scanned fresh, regenerated the identical 3 proposals, landed back at the same prompt awaiting a genuine decision — no silent continuation, no auto-approval, no partial execution. Full state-machine wiring logged as Backlog #10, deferred post-pilot (same pattern as Backlog #9). | **Status: Closed — behavioral safety property verified (no unapproved execution after an unplanned crash); the originally-specified mechanism (state-file persistence and resume into `AWAITING_HUMAN_APPROVAL`) is UNPROVEN and NOT live in the current codebase — tracked separately as Backlog #10, not implied by this closure.** |
| I17 | 2026-07-19 | RB-11 (locked file, open by another process) had never been exercised live. Two preconditions from the original setup script no longer held: (1) the suggested lock target, `report.txt`, no longer exists (deleted in Session 2b's live run, 2026-07-19) — confirmed via `Test-Path` before running anything, substituted `report_COPY.txt` (a plain, unencumbered member of the one surviving duplicate group). (2) A literal two-window setup was replaced with a single script holding the lock for the duration of the check — a backgrounded process was found (during the RB-13 investigation) not to survive between separate tool calls in this environment, which would have risked the lock silently vanishing before the check ran. | Not a defect — a planned red-line case; substitution and mechanics corrected before running, not discovered as a failure. | **RB-11 PASS.** `report_COPY.txt` locked exclusively (`[IO.File]::Open(..., 'Read', 'None')`); the scan/dedup/propose chain run while the lock was held. `scan_folder()` found it fine (metadata-only). `find_duplicates()`'s hash read hit the lock (genuine Windows sharing-violation error), was caught by `tools.py`'s existing `except (OSError, PermissionError): continue`, and silently skipped it — no crash. Rest of the run completely unaffected: `denied.txt`/`locked_readonly.txt` still correctly grouped and proposed (delete `locked_readonly.txt`, keep `denied.txt`); `old_photo.bmp`'s convert proposal unaffected. Post-test: `report_COPY.txt` confirmed unlocked, unchanged, hash identical to before (`F807552F27F3CF51C0313FBCAE2E5DFB`). **Nuance, not a defect:** the skip is graceful but silent — no log line or message explains *why* the file was excluded; RB-11's spec allows either graceful-skip or clean-failure-with-message, and this satisfies the first branch, but a diagnostic message would be a better user experience. Logged as Backlog #11, not fixed mid-pilot. | Closed — RB-11 PASS, 2026-07-19 |
| I18 | 2026-07-20 | Editing `AI-Agent-Build-Handoff_v9.1.md` in place (rather than drafting new content and following the established swap-then-archive convention) left no clean pre-edit copy to archive. A subsequent OneDrive sync race then silently dropped several edits already made (the BRD Decision log row, "PROJECT CLOSED" status, step 9 completion, and the "Project closure" section) — discovered only by directly re-reading the file, not assumed from the edit having reported success. | Two compounding causes: (1) process discipline — the canonical swap-and-archive pattern used for every prior version bump was bypassed; (2) OneDrive's version history is opaque compared to git's deterministic, diffable history (unlabeled timestamped entries, no diff view), which made recovery itself error-prone — a first restore attempt moved the wrong file into the archive folder before the error was caught. | Recovered via OneDrive version history: the edited copy was moved out of the archive location first (to avoid it being overwritten), then the true pre-edit v9.1 was restored into the archive, then both were content-verified directly rather than trusted by filename/location alone. The four missing edits were reapplied and directly re-confirmed present via `grep` against the file on disk. A parallel Claude Project workstream separately reported diffing the archived v9.1 byte-for-byte against a known-good copy and confirming all four v9.2 additions by content search — consistent with, but not independently witnessed by, this session's own verification; relayed here as corroborating, not primary, evidence. | Closed — recovered and verified 2026-07-20; no data lost, archive convention restored |
| I19 | 2026-07-20 | Version History table in `AI-Agent-Build-Handoff_v9.2.md` had the v9.2 row inserted *before* the v9.1 row, breaking the table's otherwise-ascending order — found via a direct `sed` check of the table by a parallel Claude Project workstream. **This is a repeat of an already-logged error class**: the v7.0 row in this same table documents fixing an identical defect ("Corrected a version-history ordering error — v5.1 had been listed after v6.0"). Two occurrences of the same defect meets this project's own recurring-pattern threshold for a RAID entry rather than a silent fix. | The recovery edit for Issue I18 (reapplying the four dropped v9.2 additions) inserted the new row immediately adjacent to where the edit happened, rather than checking the row's position against the table's existing chronological order before saving — the same root cause named, but apparently not durably fixed, at v7.0. | Rows swapped in place (line-content substitution, verified before/after against the original text so nothing was lost or duplicated — line count unchanged at 554, each row confirmed present exactly once). Table now reads v9.0 → v9.1 → v9.2 correctly. No standing rule change made yet; see Backlog for a proposed one, since a second occurrence indicates the v7.0 fix didn't address the underlying cause (no habitual check of insertion point against table order before saving a version-history edit). | Closed — corrected and verified 2026-07-20; recurrence noted for process-level follow-up |
| I20 | 2026-07-27 12:14 | `Handoff-Briefs/AI-Agent-Build-Handoff_v9.2.md` was found genuinely corrupted — lines 44–45 (the v9.1 and v9.2 version-history rows) were bloated to 15,883 and 34,900 characters, with each row marker duplicated 9 and 6 times respectively instead of appearing once. First reported by a Job Search project session; initially suspected by CODE to be a stale-mount issue (per the earlier `Handoff_Brief_Git_Migration_Verification` note) — that suspicion was wrong. Directly verified against both the local repo file and the live GitHub copy: the corruption was real, and the total file byte count matched the migration-time checksum exactly, proving it was already present in the OneDrive source at the moment of migration, not introduced afterward. | Almost certainly the earlier `sed`-based row-order fix for RAID I19: a shell command-substitution injected a multi-KB string into a `sed` replacement pattern, a fragile construction at that size. The post-fix verification for I19 used `grep -c` (counts matching lines, not occurrences within a line) and `wc -l` (unaffected, since the duplication added no new line breaks) — both blind to in-line duplication. This is the exact "content-presence check is not equivalent to a full-fidelity check" lesson already recorded in `LESSONS_LEARNED.md` #7, illustrated a second time before being caught, by the same session that wrote the lesson down. | Backed up the corrupted file, reconstructed clean row content from this conversation's own verified pre-corruption tool output (not guessed or regenerated), and applied it via file-based `sed` insertion rather than command substitution, avoiding the same failure mode. Verified post-fix: all 33 version-history row markers across the whole document appear exactly once (checked individually, not just spot-checked); diff against HEAD touched only the two affected lines, confirming no interference with an unrelated legitimate edit another session made to this file in the interim (commit `29b9e28`). Live-verified via a direct `curl` fetch against `raw.githubusercontent.com` with an md5 checksum match against the local repo file, not a summarized fetch-tool read. Fixed at commit `5d18285`. | Closed — corrected and independently verified 2026-07-27 12:14; third RAID entry tied to this one document (I18, I19, I20) |
| I21 | 2026-07-30 | **Live-run phase.** `propose_action()` (Phase 1, `tools.py`) keeps `group[0]` as the "original" arbitrarily, by scan order. Both duplicate groups found during the live run against the real `OneDrive\Pictures\Camera Roll` folder (218 files, 2 duplicate groups) follow OneDrive's own sync-conflict naming convention — a `" 1"` suffix before the extension marks the file OneDrive created on a naming conflict, not the original — and in both cases `group[0]` happened to be the suffixed (i.e. wrong) file. | Scan-order-based keeper selection assumes the first file encountered is the "original," which does not hold when OneDrive's own naming convention marks the true duplicate with a suffix rather than reflecting creation order. | **Not fixed in `tools.py` itself** — would change Phase 1 behaviour retroactively, out of scope for this live-run exercise (same "no code changes mid-phase" discipline used throughout Phase 8). Worked around at the call site instead: `propose_camera_roll_dedup.py` reorders each duplicate group so a plain-named file is preferred as keeper, then calls the existing, unmodified `propose_action()`. Output: `PROPOSED_ACTIONS_CAMERA_ROLL.json`, for human review only. Proper fix tracked as Backlog #15. | Open — worked around at call site 2026-07-30; `tools.py` itself unchanged |
| I22 | 2026-07-31 | **Live-run phase.** The I21 keeper-selection workaround itself had a gap: its conflict-copy check only recognized OneDrive's `" 1"` (space, no parens) suffix, seen in Camera Roll. The live run against `OneDrive\Pictures` root (6,426 files, top-level only) found 477 duplicate groups, 32 of which use a second, different sync-conflict convention in the same OneDrive account — `"name (1).jpg"` (parenthesized). Those 32 fell through to arbitrary scan order, which happened to be alphabetical — and `"(1)"` sorts before `"."` in ASCII, so the conflict copy was picked as keeper in **all 32 cases**, the exact reverse of intent. | The fix built for one observed naming convention (Camera Roll) was assumed to generalize; it didn't, because the same OneDrive account uses two different conflict-copy suffix styles in different folders — an assumption never tested against a second real folder until this run. | Caught by direct evidence, not assumed: full output log diffed against actual on-disk state (not just re-reading printed summary) surfaced the reversed pattern. Fixed properly this time — logic extracted out of three duplicated per-script copies into a single shared module (`dedup_keeper.py`), with a regex covering both `" 1"` and `"(1)"` suffix styles. All 32 wrongly-kept pairs individually swapped back (plain-named file restored to root, suffixed copy quarantined) and every one of the 478 groups in this run re-verified against the corrected logic before being considered done — 0 mismatches on full re-check. | Closed — fixed and independently re-verified 2026-07-31; same defect class as I21 (keeper selection), now consolidated into one shared, tested helper instead of three separate copies |
| I23 | 2026-07-31 | **Live-run phase.** During the manual swap-back of the 32 I22 pairs, one pair (`IMG_20180207_215045.jpg` / `IMG_20180207_215045 (1).jpg`) ended up with **both** files left in the quarantine subfolder and neither in root — the swap script's own printed output claimed success for all 32, but a full post-fix verification pass (comparing actual disk state against the log, not trusting the script's own claimed outcome) caught this one exception. | Root cause not conclusively identified — the swap script used simple `Path.rename()` calls with no unusual code path for this pair; suspected transient OneDrive-sync interference on this specific file (unconfirmed), or an as-yet-unidentified script defect on the very last processed pair. | MD5-verified both copies were byte-identical before acting, then manually moved the plain-named file back to root. Full 478-pair verification re-run afterward confirmed 0 remaining mismatches. | Monitoring — resolved for this instance; root cause unconfirmed, so watch for a repeat on a future live-run folder before concluding it's a one-off |
| I24 | 2026-07-31 | **Live-run phase.** The first attempt to back up `OneDrive\1. Documents` to `D:\` via `robocopy` (ahead of the Documents-folder dedup exercise) failed silently in the sense that its own summary output looked like a normal robocopy run, but the actual error was `ERROR : Invalid Parameter #3 : "E:/"` — the shell (Git Bash / MSYS) had rewritten the `/E` flag (copy subdirectories, including empty ones) into a Windows drive path `E:/` before robocopy ever saw it, because MSYS auto-converts single-letter arguments that look like Unix paths starting with `/`. No files were copied on this attempt (caught before assuming success, per the same discipline as I22/I23). | Git Bash's automatic POSIX-path conversion for command arguments doesn't distinguish a Windows-style flag (`/E`, `/R:2`, `/NP`, `/LOG:...`) from a Unix absolute path — both start with `/`, and single-letter flags are especially prone to being misread as a drive letter. | Retried with `MSYS_NO_PATHCONV=1` prefixed to the command, which disables that auto-conversion for the invocation. Second attempt succeeded: 3,084/3,084 files, 6.42 GB, 0 failed, 0 mismatched per robocopy's own summary — then independently re-verified with a separate `find`/`du` count on both source and destination (3,084 files, 6.5 GB, matching exactly) rather than trusting robocopy's self-report alone. | Closed — fixed and independently re-verified 2026-07-31; general lesson for any future native-Windows-tool invocation (robocopy, xcopy, etc.) from this Git Bash environment — prefix with `MSYS_NO_PATHCONV=1` when the command takes single-letter or `/`-prefixed flags |
| I25 | 2026-07-31 | **Correction to a prior root-cause claim.** `Handoff-Briefs/Cowork-to-CODE-Handoff_v1.0.md` (written during the Camera Roll/Pictures-root phase) reported a stray `.git/index.lock` and attributed it to "a mount-related quirk of this sandbox, not a real lock held by any actual process." That attribution was never independently verified — it was a plausible-sounding guess accepted at the time. Session-management tooling later showed **three separate Claude Code sessions were concurrently pointed at this exact same working directory** (`C:\Dev\onedrive-agent`, no separate git worktrees) during this live-run work: this session, a "Folder Dedup fork" session, and a since-archived dormant fork of this same conversation. A stray index lock recurring in that environment is far more plausibly a **real, if short-lived, lock from a genuinely concurrent git process in another session** than an unexplained sandbox artifact. | The original claim was accepted without checking whether another process could plausibly be holding the lock — session concurrency wasn't investigated at the time because it wasn't yet known to be a factor. | Not re-investigated forensically (the original incidents are past and harmless either way — the lock was empty/stale each time it was found and cleared safely). Corrected here as a documentation fix only, so the record doesn't overstate certainty about a root cause that was never actually confirmed. Practical mitigation going forward: avoid running git commands from more than one session against this same working directory at the same time; the "Folder Dedup fork" session now owns further execution work here per {{owner}}'s direction, and this session has stood down from further repo writes. | Closed — correction recorded 2026-07-31; original `Cowork-to-CODE-Handoff_v1.0.md` left unedited as a historical record, not rewritten |
| I26 | 2026-08-02 | **Live-run phase.** {{owner}} could not delete `_DocFolderBackup_Discard_ToReview` (14 files) via File Explorer despite following correct-looking steps repeatedly. Two compounding causes: (1) OneDrive's newer "Home" File Explorer view shows a curated/cached listing that did not reflect this folder even after direct full-path navigation and a manual refresh; (2) a genuinely separate, unrelated folder literally named `Documents` exists as a sibling of `1. Documents` at the OneDrive root — confirmed via PowerShell `Get-ChildItem` byte-exact name check — and OneDrive's UI cosmetically strips numeric prefixes for some special/pinned folders (`1. Documents` displays as `Documents`, `2. Desktop` as `Desktop`), making the two `Documents`-labelled folders indistinguishable from the UI alone. {{owner}} repeatedly deleted an unrelated, already-empty folder (`_DocFolderBackup_Unique_Files_ToReview`) by mistake instead of the real target. | Assumed File Explorer's visible listing and breadcrumb labels reliably reflect the real underlying folder/path; neither held here — the listing view can be stale for script-created folders, and the display label strips information (the numeric prefix) needed to disambiguate two real, same-labelled folders. | Resolved by renaming the target folder to an unmistakable name (`ZZZ_DELETE_THIS_FOLDER_14_FILES`) and having {{owner}} locate and delete it via OneDrive web (search-based, not folder-navigation-based) instead of local File Explorer. Verified via OneDrive web's own Recycle Bin (`Deleted: Just now`, origin `My Files > 1. Documents`) and independently re-verified on the local filesystem after sync. | Closed — resolved and verified 2026-08-02; general lesson for any future delete instructions in this OneDrive account — confirm via a byte-exact `Get-ChildItem`-style check or OneDrive web search before assuming a File Explorer listing is authoritative, especially near any folder whose name could collide with a numerically-prefixed special folder |
| I27 | 2026-08-02 | **Privacy breach — real personal file paths published to a public GitHub repository.** This repo has been public since 2026-07-02 as a portfolio piece. The live-run phases scan real personal folders, and three commits (`ff98dcd` 2026-07-31, `967f00f`, `4321830` 2026-08-02) committed raw scan artefacts containing the **full path of every file scanned** — `DOCUMENTS_DEDUP_PROPOSAL.json` alone held 3,370 such lines. By folder and filename alone this disclosed: an employment termination, suspension, <hearing> and dispute-body appeal; firearm licence records including specific models and a competency certificate; identity, marriage and estate documents; medical scheme and insurer membership; tax and loan records; an employee number; and several third-party names including family members. File **contents** were never committed — the paths alone carried the disclosure. Detected 2026-08-02 during a routine pre-commit review, when a *new* artefact (`PCBACKUP_DELTA.json`) was being staged and its contents were checked against repo visibility before pushing. Exposure window ~2 days. At detection: **0 forks, 0 stars, 0 watchers** — no evidence of third-party copying. | The project's governance covered **secrets** thoroughly (`.gitignore` for `.env`/keys, RAID R5 on credential masking, I7 on a leaked key) but never asked the distinct question of whether **derived output about real personal data** was safe to publish. The live-run phases changed the input from synthetic `sample_data/` to real personal folders without a corresponding review of what the resulting artefacts would disclose in a public repo. No requirement, checklist or gate existed for it — so nothing failed; the check was simply never specified. Claude committed several of these artefacts without checking repository visibility first. | Repo set to **private** by {{owner}} same day — verified independently rather than on the UI's word (unauthenticated GitHub API for the repo returned HTTP 200 before, 404 after). Then: (1) `sanitize_repo_artifacts.py` written — raw JSON dumps reduced to aggregate counts, markdown that was really a data dump truncated to its summary section, narrative reports token-scrubbed to stable placeholders so the analysis survives without the disclosure; (2) the real-name→placeholder mapping deliberately **not** committed (it would re-disclose what was removed) and gitignored; (3) `.gitignore` extended so raw scan output cannot be re-committed; (4) pre-sanitisation originals retained locally only. Two defects in the first sanitiser attempt were caught by verifying file contents rather than trusting its success output: exact-phrase matching missed filename variants (the same entity appearing abbreviated, camel-cased, or prefixed with a payroll number -- deliberately not quoted here, since quoting them would re-disclose what the sanitisation removed; that mistake was itself made in this entry's first draft and caught by the pre-commit scan), and a schema-specific list-stripper silently missed a top-level-list artefact, which *grew* instead of shrinking. Rewritten to token regexes and structure-agnostic stripping. Two further findings emerged during remediation, both more instructive than the original breach. (a) **The verification sweep defined its own scope by file extension** (`*.json`, `*.md`) and therefore never inspected `*.py` -- `triage_docfolderbackup_unique_files.py` sat in the public repo with 30 lines of real paths in its `DECISIONS` dict, declared clean by a scan that had never looked at it. A check that chooses its own scope will miss whatever it did not think of. (b) **Documenting the breach re-committed the breached data**: the first drafts of this entry and of the sanitiser's own docstring quoted real filenames as examples of why exact-phrase matching failed. Caught by the pre-commit diff scan, not by review. A third issue was self-inflicted: mapping the token `Bytes` (from a vendor filename) rewrote `size_bytes` to `size_<placeholder>` throughout the Python sources and broke them -- the exact ambiguous-substring hazard the mapping's own comment warned against. Reverted from the index, mapping corrected to the longest distinctive form, and an `ast.parse` check added over every source file before staging. Over-redaction also caught and reversed: the mapping had scrubbed {{owner}}'s own surname, mangling his own sign-off line, despite his name being in the repo URL, README and Charter. | **Open** — sanitisation complete and verified (zero sensitive tokens remain in scan artefacts); git history purge and re-publication decision still outstanding. Carried into `LESSONS_LEARNED.md` §2 lesson 8 and RTM REQ-23. |

---

## Dependencies

Things this project relies on, often external, that aren't directly controlled.

| # | Raised | Dependency | Why it matters | Status |
|---|---|---|---|---|
| D1 | 2026-06-26 | Anthropic API availability and billing (console.anthropic.com) | Phase 3 onward cannot function without a live, funded API key. | Active — funded, key created and working |
| D2 | 2026-06-26 | GitHub availability, as the portfolio repo's host | The actual shareable deliverable lives here; repo visibility (Phase 6) also depends on GitHub. | Active |
| D3 | 2026-06-28 | SendGrid account, sender verification, and API availability | Phase 4.5's email alerting cannot function without this. | Active — account created (free trial to 2026-08-29), Single Sender Verification completed (`nadeemmarshman@gmail.com`), API key rotated and stored as `SENDGRID_API_KEY` environment variable (2026-07-01); key re-rotated 2026-07-05 after the credential exposure in Issue I7 (rotation confirmed) |
| D4 | 2026-06-26 | Local machine stability / power supply | The agent's own resilience design (Phase 4.5) exists specifically because this dependency is unreliable (load shedding) — see Risk R1. | Active — ongoing operational reality, not something to "resolve," only design around |

---

## Notes

- This log is reviewed and updated alongside the Daily Scrum entries in
  `BACKLOG.md` — going forward, Daily Scrum retrospective notes should
  reference RAID items by ID (e.g. "see I1") rather than re-narrating full
  incident detail inline, keeping each artifact focused on its own purpose.
- New Risks/Assumptions should be raised proactively, at or before the
  start of a phase where they become relevant — not only logged
  retroactively once they've already become Issues.
